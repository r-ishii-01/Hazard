/** 操作パネル・タイムライン・HUD。［スタブ: ui 担当が実装］ */
import type { AppStore } from '../core/store';
import type { AppActions } from '../core/controller';

export function mountUI(_root: HTMLElement, _store: AppStore, _actions: AppActions): void {}
